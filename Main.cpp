#include "Reader.cpp"

int S_T = 0;

int Insert{
    Setr;
    Start(S_T);
    std::string Antstr,Bananastr;
    sRead(Antstr);
    sRead(Bananastr);
    //Ant::Showme(Antstr);
    Ant::KMP(Antstr,Bananastr)!=nullptr?sWrite(1):sWrite(0);
    Stop(S_T);
    Closew;
    return 0;
}